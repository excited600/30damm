/gatherings/{gatheringUuid}/guests:
  post:
    operationId: joinGathering
    tags:
      - Gatherings
    summary: Join the gathering as a guest
    parameters:
      - name: gatheringUuid
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: The UUID of the gathering to join
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - userUuid
            properties:
              userUuid:
                type: string
                format: uuid
                description: UUID of the user who wants to join the gathering
    responses:
      '201':
        description: Joined gathering successfully


/gatherings:
  post:
    operationId: openGathering
    tags:
      - Gatherings
    summary: Open a new gathering
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../3040-api-spec-overview.yml#/components/schemas/OpenGatheringRequest'
    responses:
      '201':
        description: Gathering open successfully
        content:
          application/json:
            schema:
              $ref: '../3040-api-spec-overview.yml#/components/schemas/OpenGatheringResponse'
/series:
  post:
    operationId: scheduleSeries
    tags:
      - Gatherings
    summary: Schedule gatherings based on host setting
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../3040-api-spec-overview.yml#/components/schemas/ScheduleSeriesRequest'
    responses:
      '201':
        description: Gatherings scheduled successfully


#  delete:
#    operationId: closeGathering
#    tags:
#      - Gatherings
#    summary: Close an gathering
#    parameters:
#      - name: gatheringId
#        in: query
#        required: true
#        schema:
#          type: string
#          format: uuid
#        description: The UUID of the gathering to be closed
#    responses:
#      '204':
#        description: Gathering closed successfully