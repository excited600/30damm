spring:
  application:
    name: 3040-test
  datasource:
    url: jdbc:postgresql://localhost:15432/3040
    username: test
    password: test
  data:
    redis:
      host: localhost
      port: 16379
      password: test
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
